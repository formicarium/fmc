#!/usr/bin/env node
// tslint:disable no-var-requires
// tslint:disable no-implict-dependencies

const cmd = require('@oclif/command')
const flush = require('@oclif/command/flush')
const handle = require('@oclif/errors/handle')

cmd.run()
.then(flush)
.catch(handle)
