describe('getNodes', () => {
  it('works for a lot of messages', () => {
    expect(1).toEqual(1)
  })
})

describe('getEdeges', () => {
  it('works for a lot of messages', () => {
    expect(1).toEqual(1)
  })
})
